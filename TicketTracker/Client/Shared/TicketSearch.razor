@inject NavigationManager NavManager

<div class="jumbotron">
    <h1 class="display-4 text-center">Welcome to Ticket Tracket</h1>
    <p class="lead text-center">Collect all your tickets that are tax deductible according to Sunat Peru.!</p>
    <p class="mt-4">
        <input @onkeydown="SearchForTrail"
        @bind="_searchTerm"
        @bind:event="oninput"
               type="text"
               placeholder="Search for a ticket..."
               class="form-control form-control-lg" />
    </p>
</div>

@code {
    private string _searchTerm = "";

    private void SearchForTrail(KeyboardEventArgs args)
    {
    }
}
